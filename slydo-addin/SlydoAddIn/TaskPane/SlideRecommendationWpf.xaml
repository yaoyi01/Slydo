<UserControl x:Class="SlydoAddIn.TaskPane.SlideRecommendationWpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 顶部渐变条 -->
        <Border Grid.Row="0" Height="2">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#6C63FF" Offset="0"/>
                    <GradientStop Color="#8B5CF6" Offset="0.5"/>
                    <GradientStop Color="#EC4899" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- 标题栏 -->
        <Grid Grid.Row="1" Margin="20,14,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Width="24" Height="24" CornerRadius="6">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#6C63FF" Offset="0"/>
                        <GradientStop Color="#8B5CF6" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="S" Foreground="White" FontWeight="Bold" FontSize="12"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Grid.Column="1" Text="Slydo 知识库" FontWeight="Bold" FontSize="16"
                       Foreground="#333333" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock x:Name="SettingsButton" Text="⚙️" FontSize="14" Margin="0,0,8,0" Foreground="#BBBBBB"
                           Cursor="Hand"/>
                <TextBlock Text="🔄" FontSize="14" Foreground="#BBBBBB" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- 搜索栏 -->
        <Grid Grid.Row="2" Margin="20,12,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1"
                    Background="White" Height="30">
                <TextBox x:Name="SearchBox" Text="" FontSize="12" Foreground="#333333"
                         BorderThickness="0" VerticalAlignment="Center"
                         Margin="8,0,0,0" Padding="2,0,0,0"/>
            </Border>
            <Border x:Name="SearchButton" Grid.Column="1" CornerRadius="6"
                    Background="#6C63FF" Height="30" Width="60"
                    Margin="8,0,0,0" Cursor="Hand">
                <TextBlock Text="搜索" FontSize="12"
                    FontWeight="Bold" Foreground="White"
                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>

        <!-- 大纲面板：动态从 API 获取 -->
        <Border x:Name="OutlinePanel" Grid.Row="3" Margin="20,10,20,0"
                Background="#F5F5F7" CornerRadius="8" BorderBrush="#E0E0E0"
                BorderThickness="1" Padding="12,10" Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="OutlineTitle" Text="推荐接下来的方向" FontSize="12" FontWeight="SemiBold"
                           Foreground="#333333" Margin="0,0,0,8"/>
                <StackPanel x:Name="OutlineDirections" Orientation="Horizontal"/>
            </StackPanel>
        </Border>

        <!-- 状态栏 -->
        <Grid Grid.Row="4" Margin="20,8,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Text="准备就绪" FontSize="11"
                       Foreground="#888888"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock x:Name="GoldBadgeText" Text="" FontSize="11" Foreground="#B8860B"
                           Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- 卡片列表 -->
        <ListBox x:Name="CardList" Grid.Row="5" Margin="20,6,20,0"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ItemContainerStyle="{StaticResource {x:Type ListBoxItem}}">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter HorizontalAlignment="Stretch" Margin="0,0,0,6"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.Resources>
        </ListBox>

        <!-- 底部 -->
        <Grid Grid.Row="6" Margin="20,0,20,8" Height="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="💡 点击卡片导入 · 悬停看大图" FontSize="10"
                       Foreground="#BBBBBB" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="5" Height="5" Fill="#009900" Margin="0,0,4,0"/>
                <TextBlock Text="已连接" FontSize="10" Foreground="#009900"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
